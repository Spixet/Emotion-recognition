# Eternix — CPU-only dependencies
# Uses DeepFace + TensorFlow for emotion detection (no GPU required)
# Install: pip install -r requirements-cpu.txt

# Core web framework
flask>=2.0.0
flask-socketio>=5.1.1
python-socketio>=5.7.2

# AI chat client (DeepSeek via OpenAI-compatible SDK)
openai>=1.0.0

# Emotion detection (CPU — DeepFace + TensorFlow)
deepface==0.0.93
tensorflow==2.10.0
keras==2.10.0
h5py==3.7.0

# Image processing
opencv-python>=4.5.0
numpy>=1.19.0
Pillow>=9.0.0

# Configuration and environment
pyyaml>=5.4.1
python-dotenv>=0.19.0

# Web and API utilities
requests>=2.27.1
flask-caching
flask-limiter

# Utilities
tqdm>=4.62.0
